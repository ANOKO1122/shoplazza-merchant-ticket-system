# 上线前代码审查报告

> 项目：support-bridge（客服桥梁系统）  
> 审查日期：2026-05-20  
> 目标环境：Linux 服务器（Docker / Node.js 18+）  
> 审查范围：`src/` 全部后端代码、`public/` 全部前端页面、配置与部署文件

---

## 一、总体评估

| 维度 | 评分 | 说明 |
|---|---|---|
| 功能完整性 | ⭐⭐⭐⭐ | 核心功能（工单、邮件、Webhook、店铺管理）均已实现 |
| 安全性 | ⭐⭐⭐ | 存在需修复的 HIGH/CRITICAL 问题 |
| 错误处理 | ⭐⭐⭐⭐ | try/catch 覆盖广泛，有日志落库，不会静默崩溃 |
| 代码质量 | ⭐⭐⭐⭐ | TypeScript strict 模式，参数化查询无 SQL 注入风险 |
| 部署就绪 | ⭐⭐⭐ | Dockerfile 可用但有小问题，docker-compose 缺依赖服务 |

**结论：修复以下 2 个 HIGH/CRITICAL 问题 + 3 个 MEDIUM 问题后可上线。**

---

## 二、CRITICAL / HIGH 问题（上线前必须修复）

### 2.1 [CRITICAL] 硬编码管理员密码

**文件：** `src/seed-admin.ts`  
**详情：** 明文硬编码了默认管理员密码 `thinkpro123`

```typescript
// 当前代码
seedAgent({
  username: 'admin',
  name: 'Admin',
  password: 'thinkpro123',  // ← 硬编码密码
  role: 'admin',
})
```

**风险：** 代码提交到 Git 后密码泄露；任何能读到源码的人都能登录后台。  
**修复：** 
```typescript
// 从环境变量读取，未设置则随机生成并打印到控制台
const pwd = process.env.SUPPORT_ADMIN_PASSWORD || crypto.randomBytes(8).toString('hex');
seedAgent({ username: 'admin', name: 'Admin', password: pwd, role: 'admin' })
  .then(() => console.log(`Admin seeded. ${process.env.SUPPORT_ADMIN_PASSWORD ? '' : 'GENERATED password: ' + pwd}`))
```

---

### 2.2 [HIGH] Session Cookie 缺少 `secure` 属性

**文件：** `src/admin-routes.ts` 第 50-56 行  
**详情：** 登录成功后设置的 session cookie 未启用 `secure: true`

```typescript
res.cookie(COOKIE_NAME, token, {
  httpOnly: true,
  sameSite: 'lax',
  path: '/',
  maxAge: 7 * 86400 * 1000,
  // 缺少 secure: true
});
```

**风险：** 若部署到 HTTPS 域名，cookie 可能被中间人攻击窃取。  
**修复：**
```typescript
secure: process.env.NODE_ENV === 'production',  // 生产环境强制 HTTPS
```

---

## 三、MEDIUM 问题（建议上线前修复）

### 3.1 [MEDIUM] 访问令牌暴露在 URL 查询参数中

**详情：** 顾客端工单链接格式为 `https://domain.com/ticket?t=TOKEN`，令牌直接出现在 URL 中。

**风险：**
- 浏览器历史记录会保存完整 URL（含令牌）
- HTTP Referer 头会将令牌泄露给第三方（如果有外部链接或图片）
- 服务器访问日志会记录完整 URL

**建议：** 改为路径参数或 POST 后 cookie 方案。如无法改动，至少确保：
- `Referrer-Policy: no-referrer` 响应头
- Nginx 日志脱敏（不记录 query string）

---

### 3.2 [MEDIUM] 缺少 API 速率限制

**详情：** 所有 API 端点均无速率限制，包括：
- `POST /api/admin/login` — 登录接口（可暴力破解）
- `POST /api/support/tickets` — 工单创建
- `POST /api/support/tickets/:id/messages` — 顾客发消息（虽有 5 条连发限制，但不限 IP 切换）

**建议：**
```bash
npm install express-rate-limit
```
对登录接口应用严格限制（如 5 次/分钟），对 API 整体应用宽松限制（如 100 次/分钟）。

---

### 3.3 [MEDIUM] Docker Compose 缺少依赖服务

**文件：** `docker-compose.support.yml`  
**详情：** compose 中只有 `support-bridge` 服务，缺少：
- PostgreSQL 数据库
- Mailpit（测试邮件接收）

**建议：** 添加 postgres 和 mailpit 服务定义，方便一键启动完整开发/测试环境。

---

### 3.4 [MEDIUM] 优雅关闭不完整

**文件：** `src/index.ts` 第 130-135 行  
**详情：** SIGTERM 处理器仅调用 `process.exit(0)`，未执行：
- 数据库连接池关闭 (`pool.end()`)
- 定时器清除 (`clearInterval`)
- 正在处理的请求等待

**建议：**
```typescript
const shutdown = async () => {
  console.log('[support-bridge] Shutting down...');
  clearInterval(emailTimer);
  clearInterval(backfillTimer);
  await pool.end();
  process.exit(0);
};
```

---

## 四、LOW 问题（可以上线后修复）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 4.1 | Dockerfile 第 5 行 `&&` 应改为 `;` | `Dockerfile:5` | `RUN if [ -f src/index.ts ]; then npm run build; fi && npm prune --production` — 即使 build 失败也会 prune，建议用 `;` 分隔或 && 链 |
| 4.2 | Health check 端点不检查 DB 连通性 | `src/index.ts:109` | `/health` 仅返回静态信息，建议增加 `SELECT 1` 探针 |
| 4.3 | `.env` 文件未加入 `.gitignore` | 根目录 | 确认 `.env` 已在 `.gitignore` 中，避免泄露本地配置 |
| 4.4 | 前端页面无 CSP / X-Frame-Options 头 | `public/*.html` | 建议添加 `helmet` 中间件 |
| 4.5 | 日志中可能输出完整 token | 多处 `console.log` | 邮件发送日志中使用 `maskClientLink()` 脱敏，但其他地方可能有遗漏 |
| 4.6 | 管理员预览页面 (`admin-ticket-preview.html`) 无登录验证 | `public/admin-ticket-preview.html` | 确认该页面路径不在 `router.use(requireAgent)` 之后，是否需要独立鉴权 |
| 4.7 | TypeScript `strict: true` 但未启用 `noUnusedLocals` | `tsconfig.json` | 建议启用 `noUnusedLocals: true` 和 `noUnusedParameters: true` |

---

## 五、安全检查清单

| 检查项 | 状态 | 备注 |
|---|---|---|
| SQL 注入 | ✅ 安全 | 全部使用参数化查询 `$1, $2, ...` |
| XSS（跨站脚本） | ✅ 基本安全 | 前端有 `escapeHtml()` 函数，服务端邮件模板渲染也做了转义 |
| CSRF | ⚠️ 部分 | Session cookie 是 `SameSite: Lax`，但无 CSRF token。管理后台 API 建议加 CSRF 保护 |
| 密码存储 | ✅ 安全 | `crypto.scryptSync` + 随机盐值 |
| Token 存储 | ✅ 安全 | SHA-256 哈希存储，原文不可逆 |
| Access Token 加密 | ✅ 安全 | 店铺 `access_token` 使用 AES-256-GCM 加密存储 |
| 敏感信息日志 | ⚠️ 需检查 | 确认生产环境 Nginx 日志配置脱敏 query string |
| HTTPS | ⚠️ | Cookie `secure` 属性缺失（见 2.2） |
| 依赖安全 | 未检查 | 建议上线前运行 `npm audit` |

---

## 六、上线前操作清单

### 6.1 环境变量配置

生产环境需设置以下环境变量（`docker-compose.support.yml` 或 `.env`）：

```bash
# 必填
SUPPORT_PUBLIC_BASE_URL=https://support.yourdomain.com   # ← 改为实际域名
SUPPORT_DATABASE_URL=postgres://user:password@host:5432/dbname
SUPPORT_TOKEN_SECRET=<生成64位随机字符串>
SUPPORT_MAIL_FROM="Support <noreply@yourdomain.com>"
SUPPORT_SMTP_HOST=smtp.yourprovider.com
SUPPORT_SMTP_PORT=587
SUPPORT_SMTP_USER=your_smtp_user
SUPPORT_SMTP_PASS=your_smtp_password
SUPPORT_SMTP_SECURE=false

# 推荐设置
SUPPORT_ADMIN_PASSWORD=<强密码>     # 见 2.1 修复后
NODE_ENV=production
```

### 6.2 数据库初始化

```bash
# 首次部署时创建管理员
npm run seed-admin  # 或 npx tsx src/seed-admin.ts
```

### 6.3 上线后操作

1. 登录后台 → **系统设置** → 填写公网域名
2. **店铺管理** → 添加店铺并注册 Webhook
3. **订单邮件管理** → 开启自动发送和自动兜底
4. **邮件模板** → 确认模板内容符合品牌风格
5. 测试完整流程：下单 → 收到邮件 → 创建工单 → 客服回复

### 6.4 监控建议

- 设置 `/health` 端点监控（可用性检查）
- 关注 `support_email_jobs` 表中 `failed` 状态的记录
- 关注 `support_operation_logs` 中的 `failed` 操作

---

## 七、项目文件清单

| 类型 | 文件 | 用途 |
|---|---|---|
| 入口 | `src/index.ts` | Express 启动、CORS、静态文件、定时器 |
| 配置 | `src/config.ts` | 环境变量加载、店铺配置 |
| 数据库 | `src/pg.ts` | PostgreSQL 连接池、表结构初始化 |
| 认证 | `src/auth.ts` | 管理员登录/会话管理 |
| 令牌 | `src/token.ts` | 顾客访问令牌（bt_/ta_）生成与验证 |
| 工单 | `src/ticket-service.ts` | 工单 CRUD、消息管理 |
| 顾客API | `src/support-routes.ts` | 顾客端 `/api/support/*` |
| 管理API | `src/admin-routes.ts` | 管理端 `/api/admin/*`（含 Webhook） |
| 邮件 | `src/email-service.ts` | 邮件模板、任务创建、发送、预设 |
| 邮件发送 | `src/mailer.ts` | Nodemailer 封装（Mailpit 切换） |
| 店铺 | `src/store-service.ts` | 店铺 CRUD、Token 加密 |
| Shoplazza | `src/shoplazza-client.ts` | 订单 API、Webhook 注册/注销 |
| Webhook | `src/shoplazza-webhook-routes.ts` | 接收 Shoplazza Webhook 推送 |
| 订单标准化 | `src/normalize-order.ts` | 订单数据标准化 |
| 兜底同步 | `src/backfill-jobs.ts` | 定时拉取订单、补全快照与邮件 |
| 日志 | `src/log-service.ts` | 同步日志 + 操作日志 |
| 前端 | `public/*.html` | 顾客端/管理端全部页面 |
| 部署 | `Dockerfile`, `docker-compose.support.yml` | 容器化部署 |

---

*报告结束。建议优先处理 2.1 和 2.2，再处理 3.1-3.4，其余可上线后迭代。*
