# 工单对话发送图片功能 — 优化实施方案

> 基于原始方案 `对话发送图片拓展.md` 制定，补充了管理员端适配、与现有系统的接口对齐、完备的安全措施和上线检查清单。

---

## 一、改动范围总览

| 层级 | 文件 | 改动类型 |
|---|---|---|
| 数据库 | `src/pg.ts` | 新增 ALTER TABLE（attachments 列 + content DROP NOT NULL）+ 存量 NULL 回填 |
| 核心逻辑 | `src/ticket-service.ts` | `addCustomerMessage` / `addAgentMessage` / `getTicketMessages` 增加 attachments 支持 |
| 新增文件 | `src/storage-service.ts` | 新建，封装 multer + sharp + StorageProvider 接口 |
| 顾客端 API | `src/support-routes.ts` | POST 发消息接入 multer；GET 消息映射增加 attachments 字段 |
| 管理员 API | `src/admin-routes.ts` | POST 发消息接入 multer；GET 消息映射增加 attachments；DELETE 消息级联清理附件 |
| 管理员预览 | `src/admin-preview.ts` | `buildCustomerPreviewResponse` 消息映射增加 attachments |
| 服务入口 | `src/index.ts` | 新增 `/uploads` 静态目录 + multer 全局错误处理中间件 |
| 部署 | `docker-compose.support.yml` | `support-bridge` 增加 uploads volume |
| 前端 | `public/ticket.html` | 输入框改造（FormData + 图片预览 + 消息渲染 attachments） |
| 前端 | `public/admin-ticket-preview.html` | 同上（管理员预览回复页） |
| 前端 | `public/admin-base.css` | 新增图片消息样式 |
| 新增依赖 | `package.json` | `multer` `sharp` `@types/multer` |

---

## 二、存储策略：本地持久化 + 预留 OSS 抽象

### 2.1 StorageProvider 接口

```typescript
// src/storage-service.ts

export interface StorageProvider {
  /** 从临时文件路径读取并压缩保存，返回相对 URL 路径（如 /uploads/2026/abc.webp） */
  save(tempFilePath: string, originalName: string): Promise<string>;
  /** 按相对路径删除文件 */
  delete(relativeUrl: string): Promise<void>;
  /** 获取存储目录总大小（字节），用于管理后台监控 */
  getTotalSize(): Promise<number>;
}
```

### 2.2 当前实现：LocalStorageProvider

- 图片落盘到 `{项目根目录}/public/uploads/{YYYY}/{hex}.webp`
- 经 Sharp 压缩后单张通常 50–150KB
- 按每周 10 张图估算，一年约 50MB，对服务器毫无压力

### 2.3 未来迁移路径

当业务量暴增时，只需实现一个 `S3StorageProvider` 或 `OssStorageProvider`，替换 `storage-service.ts` 中的导出实例即可。上层业务代码（ticket-service、routes）只需要知道返回的 URL 路径，完全不依赖存储底层。

---

## 三、压缩策略：后端 Sharp 统一处理

| 参数 | 值 | 理由 |
|---|---|---|
| 原图上限 | 5MB | Multer `limits.fileSize` 硬限制（iPhone 照片通常 2–4MB，5MB 足够覆盖售后场景） |
| 允许类型 | `image/jpeg` `image/png` `image/webp` | 仅静态图片，防 SVG/HTML 伪装 |
| 输出格式 | WebP | 压缩率极高，2026 年浏览器支持率 >97% |
| 最大宽度 | 1200px | 售后场景（订单截图、破损照片）不需要 4K |
| 质量 | 80 | 清晰度与体积的最佳平衡点 |
| EXIF 方向 | `sharp().rotate()` | 自动修正手机竖拍方向 |
| 文件名 | `crypto.randomBytes(16).toString('hex').webp` | 32 位十六进制，防遍历、防碰撞 |

> **为什么不在前端压缩？** 前端 Canvas 压缩容易被绕过（curl 直接发原图），且不同设备压缩效果不一致。后端统一处理是唯一可靠方案。

### 三.1 性能优化：diskStorage + 流式处理（防内存溢出）

**风险场景：** 如果使用 `multer.memoryStorage()`，3 张 5MB 图片 + Sharp 中间缓冲会瞬间占用约 50–75MB 堆内存。3–5 个用户同时上传可能导致频繁 GC 甚至 OOM。

**优化策略：**

| 层面 | 方案 | 效果 |
|---|---|---|
| 文件接收 | `multer.diskStorage()` 写入 `/tmp/support-uploads` | 大文件不进入 Node.js 堆内存 |
| 图片读取 | `sharp(filePath)` 从磁盘流式读入 | Sharp 内部按需缓冲，仅占用解码所需内存（约 10–20MB） |
| 限制收紧 | 原图上限从 10MB → 5MB | 进一步降低单次请求的内存压力 |
| 临时清理 | 处理完成后 `fs.unlink(tempPath)` | 不残留临时文件 |

---

## 四、数据库变更

### 4.1 表结构变更

在 `src/pg.ts` 的 `ensureTables()` 函数末尾追加：

```sql
-- 新增附件列
ALTER TABLE support_ticket_messages
  ADD COLUMN IF NOT EXISTS attachments JSONB DEFAULT '[]'::jsonb;

-- 允许纯图片消息（content 可为空字符串，兼容现有 NOT NULL 约束）
-- 如果希望完全允许 NULL，可追加：
-- ALTER TABLE support_ticket_messages ALTER COLUMN content DROP NOT NULL;
```

> **存量数据处理**：`ADD COLUMN IF NOT EXISTS` 的 `DEFAULT` 只对新插入行生效，已有消息的 `attachments` 列为 `NULL`。启动后应执行回填：
> ```sql
> UPDATE support_ticket_messages SET attachments = '[]'::jsonb WHERE attachments IS NULL;
> ```
> 前端代码已通过 `msg.attachments && msg.attachments.length > 0` 兼容 `NULL`，但数据库层面统一为 `[]` 更干净，也方便后续 JSONB 查询。
>
> **content 列说明**：现有表定义 `content TEXT NOT NULL`。方案允许纯图片消息时传空字符串 `''`，这不会违反 `NOT NULL`（空字符串 ≠ SQL NULL）。如需更严谨的语义，可 `ALTER COLUMN content DROP NOT NULL`。

### 4.2 存储格式

```json
["/uploads/2026/06/a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6.webp"]
```

- 始终为字符串数组
- 无图片时为 `[]`
- 路径相对于服务根，前端可直接拼接域名

### 4.3 兼容性

- `DEFAULT '[]'` 保证存量消息不受影响
- `getTicketMessages` 查询中增加 `attachments` 列即可
- 不创建额外索引（JSONB 数组路径查询目前无需求）

---

## 五、后端改动详情

### 5.1 新增 `src/storage-service.ts`

```typescript
import multer from 'multer';
import sharp from 'sharp';
import path from 'path';
import fs from 'fs/promises';
import crypto from 'crypto';

// ─── StorageProvider 接口 ───

export interface StorageProvider {
  /** 从临时文件路径读取并压缩保存，返回相对 URL 路径（如 /uploads/2026/abc.webp） */
  save(tempFilePath: string, originalName: string): Promise<string>;
  /** 按相对路径删除文件 */
  delete(relativeUrl: string): Promise<void>;
  /** 获取存储目录总大小（字节），用于管理后台监控 */
  getTotalSize(): Promise<number>;
}

// ─── LocalStorageProvider 实现 ───

const UPLOAD_ROOT = path.resolve(__dirname, '..', 'public', 'uploads');

export class LocalStorageProvider implements StorageProvider {
  async save(tempFilePath: string, _originalName: string): Promise<string> {
    const year = String(new Date().getFullYear());
    const dir = path.join(UPLOAD_ROOT, year);
    await fs.mkdir(dir, { recursive: true });

    const filename = `${crypto.randomBytes(16).toString('hex')}.webp`;
    const outputPath = path.join(dir, filename);

    // 从磁盘流式读取 → 流式转码 → 写入目标（内存 O(1)，不受原图大小影响）
    let transform = sharp(tempFilePath).rotate(); // 修正 EXIF 方向

    const metadata = await transform.metadata();
    if (metadata.width && metadata.width > 1200) {
      transform = transform.resize(1200);
    }

    await transform.webp({ quality: 80 }).toFile(outputPath);
    return `/uploads/${year}/${filename}`;
  }

  async delete(relativeUrl: string): Promise<void> {
    // 安全校验：路径必须以 /uploads/ 开头，防目录穿越
    if (!relativeUrl.startsWith('/uploads/')) {
      console.warn('[storage] 非法删除路径:', relativeUrl);
      return;
    }
    // 去掉 /uploads/ 前缀，防止 path.join 被绝对路径重置
    // （path.join 遇到绝对路径段会丢弃前面的路径，导致解析到系统根目录）
    const relative = relativeUrl.replace(/^\/uploads\//, '');
    // 二次校验：防止路径穿越字符
    if (!relative || relative.includes('..') || relative.includes('\0')) {
      console.warn('[storage] 路径穿越检测:', relativeUrl);
      return;
    }
    const filePath = path.join(UPLOAD_ROOT, relative);
    // 最终安全校验：解析后路径必须在 UPLOAD_ROOT 内
    if (!filePath.startsWith(UPLOAD_ROOT + path.sep) && filePath !== UPLOAD_ROOT) {
      console.warn('[storage] 路径越界:', relativeUrl, '→', filePath);
      return;
    }
    try {
      await fs.unlink(filePath);
    } catch (e: any) {
      if (e.code !== 'ENOENT') {
        console.error('[storage] 删除文件失败:', e.message);
      }
    }
  }

  async getTotalSize(): Promise<number> {
    return getDirSize(UPLOAD_ROOT);
  }
}

async function getDirSize(dir: string): Promise<number> {
  let total = 0;
  try {
    const entries = await fs.readdir(dir, { withFileTypes: true });
    for (const entry of entries) {
      const full = path.join(dir, entry.name);
      if (entry.isDirectory()) {
        total += await getDirSize(full);
      } else if (entry.isFile()) {
        const stat = await fs.stat(full);
        total += stat.size;
      }
    }
  } catch {
    // 目录不存在则返回 0
  }
  return total;
}

// ─── 单例 ───

let _provider: StorageProvider;
export function getStorageProvider(): StorageProvider {
  if (!_provider) {
    _provider = new LocalStorageProvider();
  }
  return _provider;
}

// ─── Multer 中间件 ───

const TEMP_DIR = path.resolve(__dirname, '..', 'tmp', 'support-uploads');

// 确保临时目录存在
fs.mkdir(TEMP_DIR, { recursive: true }).catch((e) => {
  console.error('[storage] 无法创建临时目录:', TEMP_DIR, e.message);
});

const ALLOWED_MIMETYPES = [
  'image/jpeg',
  'image/png',
  'image/webp',
];

export const imageUpload = multer({
  storage: multer.diskStorage({
    destination: TEMP_DIR,
    filename: (_req, file, cb) => {
      // 随机文件名防冲突
      const ext = path.extname(file.originalname) || '.jpg';
      cb(null, `${crypto.randomBytes(16).toString('hex')}${ext}`);
    },
  }),
  limits: {
    fileSize: 5 * 1024 * 1024, // 5MB（售后照片足够，同时防内存压力）
    files: 3,                   // 每次最多 3 张
  },
  fileFilter: (_req, file, cb) => {
    if (ALLOWED_MIMETYPES.includes(file.mimetype)) {
      cb(null, true);
    } else {
      cb(new Error(`不支持的文件类型: ${file.mimetype}。仅允许 JPEG/PNG/WEBP。`));
    }
  },
});

/** 顾客/管理员发消息时使用的 multer 中间件（字段名 "images"，最多 3 张） */
export const uploadAttachments = imageUpload.array('images', 3);

/** 处理 multer 文件数组，返回相对 URL 数组。处理完自动清理临时文件 */
export async function processAttachments(
  files: Express.Multer.File[],
): Promise<string[]> {
  const provider = getStorageProvider();
  const urls: string[] = [];
  for (const file of files) {
    try {
      // 传入临时文件路径，由 Sharp 从磁盘流式读取
      const url = await provider.save(file.path, file.originalname);
      urls.push(url);
    } catch (e: any) {
      console.error('[storage] 图片处理失败:', e.message);
      // 单张失败不阻塞其他图片
    } finally {
      // 无论成功与否，清理 Multer 写入的临时文件
      fs.unlink(file.path).catch(() => {});
    }
  }
  return urls;
}
```

### 5.2 修改 `src/ticket-service.ts`

#### 5.2.1 `addCustomerMessage` 增加 attachments

```typescript
export async function addCustomerMessage(params: {
  publicTicketNo: string;
  customerEmail: string;
  customerName?: string;
  content: string;
  attachments?: string[];  // 新增
}) {
  // 校验：文字和图片至少提供一个
  const hasText = params.content && params.content.trim().length > 0;
  const hasImages = params.attachments && params.attachments.length > 0;
  if (!hasText && !hasImages) {
    throw new Error('Message content or image is required');
  }
  if (hasText && params.content.length > MAX_MESSAGE_CONTENT_LENGTH) {
    throw new Error(`Message must be under ${MAX_MESSAGE_CONTENT_LENGTH} characters`);
  }

  const consecutiveCount = await countConsecutiveCustomerMessages(params.publicTicketNo);
  if (consecutiveCount >= 5) {
    throw new Error('You have reached the maximum of 5 consecutive messages. Please wait for a merchant response.');
  }

  const imageUrls = params.attachments || [];

  await query(
    `INSERT INTO support_ticket_messages
      (public_ticket_no, sender_type, sender_email, sender_name, content, attachments)
     VALUES ($1, 'customer', $2, $3, $4, $5)`,
    [
      params.publicTicketNo,
      params.customerEmail,
      params.customerName || null,
      params.content || '',
      JSON.stringify(imageUrls),
    ],
  );

  await query(
    `UPDATE support_tickets
     SET last_customer_message_at = now(), status = 'open', updated_at = now()
     WHERE public_ticket_no = $1`,
    [params.publicTicketNo],
  );
}
```

#### 5.2.2 `addAgentMessage` 增加 attachments

```typescript
export async function addAgentMessage(params: {
  publicTicketNo: string;
  agentId: number;
  agentName: string;
  content: string;
  attachments?: string[];  // 新增
}) {
  const hasText = params.content && params.content.trim().length > 0;
  const hasImages = params.attachments && params.attachments.length > 0;
  if (!hasText && !hasImages) {
    throw new Error('Message content or image is required');
  }

  const imageUrls = params.attachments || [];

  await query(
    `INSERT INTO support_ticket_messages
      (public_ticket_no, sender_type, sender_email, sender_name, content, attachments)
     VALUES ($1, 'agent', $2, $3, $4, $5)`,
    [
      params.publicTicketNo,
      `agent:${params.agentId}`,
      params.agentName,
      params.content || '',
      JSON.stringify(imageUrls),
    ],
  );

  await query(
    `UPDATE support_tickets
     SET last_agent_message_at = now(), status = 'waiting', updated_at = now()
     WHERE public_ticket_no = $1`,
    [params.publicTicketNo],
  );
}
```

#### 5.2.3 `getTicketMessages` 查询增加 attachments

```typescript
export async function getTicketMessages(publicTicketNo: string) {
  const r = await query(
    `SELECT id, sender_type, sender_email, sender_name, content, attachments, created_at
     FROM support_ticket_messages
     WHERE public_ticket_no = $1
     ORDER BY created_at ASC`,
    [publicTicketNo],
  );
  return r.rows;
}
```

### 5.3 修改 `src/support-routes.ts` — 顾客端发消息

当前接口位于 `POST /api/support/tickets/:publicTicketNo/messages`，需要从 `application/json` 切换到 `multipart/form-data`。

改动点：
1. 引入 `uploadAttachments` 和 `processAttachments`
2. 路由中先跑 multer 中间件，再从 `req.body` 取文字字段
3. `token` 从 FormData 字段中取（`req.body.token` 仍可用——multer 会解析文本字段到 `req.body`）

```typescript
import { uploadAttachments, processAttachments } from './storage-service';

// POST /api/support/tickets/:publicTicketNo/messages
router.post('/tickets/:publicTicketNo/messages', uploadAttachments, async (req, res) => {
  try {
    const { publicTicketNo } = req.params;
    const { token, content } = req.body || {};
    const contentStr = (content || '').trim();
    const files = req.files as Express.Multer.File[] | undefined;

    // 校验：文字和图片至少提供一个
    if (!contentStr && (!files || files.length === 0)) {
      res.status(400).json({ ok: false, error: 'Please provide message content or an image' });
      return;
    }

    if (!token) {
      res.status(401).json({ ok: false, error: 'Invalid or expired token' });
      return;
    }

    const payload = await verifyAccessToken(token);
    if (!payload || payload.purpose !== 'ticket_access') {
      res.status(401).json({ ok: false, error: 'Invalid or expired token' });
      return;
    }

    if (payload.public_ticket_no !== publicTicketNo) {
      res.status(401).json({ ok: false, error: 'Invalid or expired token' });
      return;
    }

    const ticket = await getTicketByPublicNo(publicTicketNo);
    if (!ticket) {
      res.status(404).json({ ok: false, error: 'Dispute not found' });
      return;
    }

    if (ticket.status === 'closed') {
      res.status(400).json({ ok: false, error: 'This dispute has been closed' });
      return;
    }

    // 处理图片
    let attachmentUrls: string[] = [];
    if (files && files.length > 0) {
      attachmentUrls = await processAttachments(files);
    }

    await addCustomerMessage({
      publicTicketNo,
      customerEmail: payload.customer_email,
      content: contentStr,
      attachments: attachmentUrls,
    });

    const consecutiveCount = await countConsecutiveCustomerMessages(publicTicketNo);
    res.json({ ok: true, remainingMessages: Math.max(0, 5 - consecutiveCount) });
  } catch (e: any) {
    if (e.message && e.message.includes('maximum of 5 consecutive')) {
      res.status(429).json({ ok: false, error: e.message });
      return;
    }
    if (e.message && e.message.includes('Message must be under')) {
      res.status(400).json({ ok: false, error: e.message });
      return;
    }
    if (e.message && e.message.includes('不支持的文件类型')) {
      res.status(400).json({ ok: false, error: e.message });
      return;
    }
    res.status(500).json({ ok: false, error: e.message });
  }
});
```

### 5.4 修改 `src/admin-routes.ts` — 管理员端发消息

当前接口 `POST /api/admin/tickets/:publicTicketNo/messages`：

```typescript
import { uploadAttachments, processAttachments } from './storage-service';

// POST /api/admin/tickets/:publicTicketNo/messages
router.post('/tickets/:publicTicketNo/messages', uploadAttachments, async (req, res) => {
  try {
    const agent = await getAgent(req);
    if (!agent) {
      res.status(401).json({ ok: false, error: '未登录' });
      return;
    }

    const { content } = req.body || {};
    const contentStr = (content || '').trim();
    const files = req.files as Express.Multer.File[] | undefined;

    if (!contentStr && (!files || files.length === 0)) {
      res.status(400).json({ ok: false, error: '请提供消息内容或图片' });
      return;
    }

    const ticket = await getTicketByPublicNo(req.params.publicTicketNo);
    if (!ticket) {
      res.status(404).json({ ok: false, error: '工单不存在' });
      return;
    }

    if (ticket.status === 'closed') {
      res.status(400).json({ ok: false, error: '工单已关闭' });
      return;
    }

    // 处理图片
    let attachmentUrls: string[] = [];
    if (files && files.length > 0) {
      attachmentUrls = await processAttachments(files);
    }

    await addAgentMessage({
      publicTicketNo: req.params.publicTicketNo,
      agentId: agent.agentId,
      agentName: agent.name,
      content: contentStr,
      attachments: attachmentUrls,
    });

    await ensureAgentReplyNoticeJob({
      storeSubdomain: ticket.store_subdomain,
      storeName: ticket.store_name,
      storeDomain: buildStoreDomain(ticket.store_subdomain),
      orderId: ticket.order_id,
      orderNumber: ticket.order_number,
      customerEmail: ticket.customer_email,
      publicTicketNo: ticket.public_ticket_no,
    });

    res.json({ ok: true });
  } catch (e: any) {
    if (e.message && e.message.includes('不支持的文件类型')) {
      res.status(400).json({ ok: false, error: e.message });
      return;
    }
    res.status(500).json({ ok: false, error: e.message });
  }
});
```

### 5.5 修改 `src/index.ts` — 静态目录 + 请求体限制

在 `app.use(express.json({ limit: '512kb' }))` 之后添加：

```typescript
// multer 处理 multipart 上传时使用 diskStorage，大小限制 5MB
// JSON 请求体限制保持 512KB，互相独立

// 挂载上传文件静态目录
app.use('/uploads', express.static(path.resolve(__dirname, '..', 'public', 'uploads'), {
  maxAge: '7d',          // 图片缓存 7 天
  immutable: true,       // 文件名含随机 hex，内容永不变
}));
```

### 5.6 修改 `src/pg.ts` — 数据库迁移

在 `ensureTables()` 函数中 `support_ticket_messages` 建表语句之后添加：

```typescript
// 阶段：图片附件支持
await p.query(`
  ALTER TABLE support_ticket_messages
    ADD COLUMN IF NOT EXISTS attachments JSONB DEFAULT '[]'::jsonb
`);
```

### 5.7 API 响应中透传 attachments（⚠️ 3 处必须同步修改）

`getTicketMessages` 查询返回了 `attachments` 列，但以下 **3 处** API 响应映射目前丢弃了该字段，前端将收不到图片 URL：

#### 5.7.1 `src/support-routes.ts` — 顾客端 GET 工单详情

当前代码（约第 246 行）消息映射：

```typescript
messages: messages.map((m: any) => ({
  id: m.id,
  sender_type: m.sender_type,
  sender_name: m.sender_name,
  content: m.content,
  created_at: m.created_at,
  // ⚠️ 必须新增：
  attachments: m.attachments || [],
})),
```

#### 5.7.2 `src/admin-routes.ts` — 管理员 GET 工单详情

当前代码（约第 468 行）消息映射，同上需加 `attachments: m.attachments || []`。

#### 5.7.3 `src/admin-preview.ts` — `buildCustomerPreviewResponse`

当前代码（约第 44 行）消息映射，同上需加 `attachments: m.attachments || []`。

> 另外，顾客端 GET 工单详情的消息映射还缺少 `sender_email` 字段，建议一并补齐：`sender_email: m.sender_email || null`。

### 5.8 Multer 全局错误处理中间件

Multer 的错误（文件过大 `LIMIT_FILE_SIZE`、数量超限 `LIMIT_FILE_COUNT`、类型不符）发生在中间件层，当前 Express 没有全局错误处理中间件来捕获它们，客户端会收到不友好的 500 错误。

在 `src/index.ts` 的所有路由注册之后、`app.listen` 之前，添加以下错误处理中间件：

```typescript
// ── Multer / 通用错误处理中间件（必须在所有路由之后注册） ──
app.use((err: any, _req: express.Request, res: express.Response, _next: express.NextFunction) => {
  // Multer 文件大小超限
  if (err.code === 'LIMIT_FILE_SIZE') {
    res.status(413).json({ ok: false, error: 'File too large. Maximum size is 5MB per file.' });
    return;
  }
  // Multer 文件数量超限
  if (err.code === 'LIMIT_FILE_COUNT') {
    res.status(400).json({ ok: false, error: 'Too many files. Maximum is 3 per message.' });
    return;
  }
  // Multer 文件类型不符
  if (err.code === 'LIMIT_UNEXPECTED_FILE') {
    res.status(400).json({ ok: false, error: 'Unexpected file field. Use "images" as the field name.' });
    return;
  }
  // 自定义文件类型错误
  if (err.message && err.message.includes('不支持的文件类型')) {
    res.status(400).json({ ok: false, error: err.message });
    return;
  }
  // 其他未预期错误
  console.error('[support-bridge] 未捕获错误:', err);
  res.status(500).json({ ok: false, error: 'Internal server error' });
});
```

> **注意**：Express 的错误处理中间件必须**恰好 4 个参数** `(err, req, res, next)`，Express 靠参数数量识别。

---

## 六、前端改动详情

### 6.1 `public/ticket.html` — 顾客端

#### 输入框改造

- 在发送按钮旁边增加 📷 图标按钮
- 点击触发隐藏的 `<input type="file" multiple accept="image/*">`
- 选中文件后，在输入区域下方渲染缩略图预览（200×200，圆角）
- 每张图片右上角有 `[×]` 按钮可移除

#### 发送逻辑

```javascript
// 替代原来的 fetch JSON 调用
async function sendMessage() {
  const text = messageInput.value.trim();
  const hasText = text.length > 0;
  const hasImages = selectedFiles.length > 0;

  if (!hasText && !hasImages) return;

  const formData = new FormData();
  formData.append('token', currentAccessToken);
  formData.append('content', text);
  selectedFiles.forEach(file => formData.append('images', file));

  const resp = await fetch(`/api/support/tickets/${ticketNo}/messages`, {
    method: 'POST',
    body: formData, // 浏览器自动设置 Content-Type 为 multipart/form-data
  });

  // ... 处理响应
}
```

#### 消息渲染

```javascript
// 渲染消息时检查 attachments
function renderMessage(msg) {
  let html = '';
  if (msg.content) {
    html += `<div class="msg-text">${escapeHtml(msg.content)}</div>`;
  }
  if (msg.attachments && msg.attachments.length > 0) {
    html += '<div class="msg-images">';
    msg.attachments.forEach(url => {
      html += `<img src="${url}" class="chat-img" onclick="viewImage('${url}')" loading="lazy" />`;
    });
    html += '</div>';
  }
  return html;
}
```

### 6.2 `public/admin-ticket-preview.html` — 管理员预览回复页

同顾客端改造逻辑，发送时使用管理员的 API 端点 `/api/admin/tickets/.../messages`。

注意：管理员端不需要 `token` 字段认证（用 Cookie session），所以 FormData 只需 `content` + `images`。

### 6.3 `public/admin-base.css` — 通用图片消息样式

```css
/* 聊天消息中的图片 */
.msg-images {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 8px;
}

.chat-img {
  width: 200px;
  height: 200px;
  object-fit: cover;
  border-radius: 8px;
  cursor: pointer;
  border: 1px solid #e8e8e8;
  transition: transform 0.2s;
}

.chat-img:hover {
  transform: scale(1.05);
}

/* 图片预览遮罩 */
.image-lightbox {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  cursor: pointer;
}

.image-lightbox img {
  max-width: 90vw;
  max-height: 90vh;
  object-fit: contain;
  border-radius: 4px;
}

/* 图片上传预览区 */
.image-preview-area {
  display: flex;
  gap: 8px;
  padding: 8px 0;
  flex-wrap: wrap;
}

.image-preview-item {
  position: relative;
  width: 80px;
  height: 80px;
  border-radius: 6px;
  overflow: hidden;
  border: 1px solid #d9d9d9;
}

.image-preview-item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.image-preview-item .remove-btn {
  position: absolute;
  top: 2px;
  right: 2px;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.5);
  color: #fff;
  border: none;
  font-size: 12px;
  line-height: 20px;
  text-align: center;
  cursor: pointer;
}
```

---

## 七、Docker Compose 部署变更

修改 `docker-compose.support.yml`，为 `support-bridge` 服务增加 uploads volume：

```yaml
services:
  support-bridge:
    build: .
    image: support-bridge:local
    restart: always
    ports:
      - "4001:4001"
    depends_on:
      - postgres
      - mailpit
    env_file:
      - .env
    volumes:
      # 图片持久化：宿主机 ./uploads 目录映射到容器内 /app/public/uploads
      # 确保 docker compose down 重建容器后用户上传的图片不丢失
      - ./uploads:/app/public/uploads
    networks:
      - support_net
```

> **不需要修改 nginx 配置**：当前 `support-nginx.conf` 已用 `location /` 反代所有请求到 `support-bridge:4001`，`/uploads/` 路径的图片请求会自动透传到 Express。

---

## 八、安全措施

| 层面 | 措施 | 说明 |
|---|---|---|
| **类型白名单** | `image/jpeg` `image/png` `image/webp` | 拒绝 GIF/SVG（可含脚本）、HTML 伪装等 |
| **大小硬限制** | Multer `limits.fileSize = 5MB` | diskStorage 在落盘阶段拒绝超大文件 |
| **数量限制** | `upload.array('images', 3)` | 每条消息最多 3 张 |
| **后端压缩** | Sharp 强制转 WebP/1200px | 即使绕过前端检查，也无法上传恶意原始文件（会被转码） |
| **随机文件名** | `crypto.randomBytes(16)` hex（临时 + 最终双随机） | 防止路径遍历、猜测 URL |
| **路径安全** | `delete()` 方法校验路径前缀 `/uploads/` | 防止通过 `../../../etc/passwd` 删除系统文件 |
| **临时文件清理** | `processAttachments` 的 `finally` 块中 `fs.unlink` | 即使转码失败也不残留原始文件在 `/tmp` |
| **CSRF 防护** | 顾客端需要 `token` 认证，管理员端需要 Cookie session | 上传接口同样受鉴权保护 |

---

## 九、实施步骤

| 步骤 | 内容 | 验证方式 |
|---|---|---|
| 1 | 安装依赖 `npm install multer sharp @types/multer` | `npm ls multer sharp` |
| 2 | 创建 `src/storage-service.ts` | TypeScript 编译通过 |
| 3 | 修改 `src/pg.ts` 加 ALTER TABLE + 存量 NULL 回填 | 启动后检查 `\d support_ticket_messages` 和 `SELECT count(*) FROM support_ticket_messages WHERE attachments IS NULL` |
| 4 | 修改 `src/ticket-service.ts`（3 个函数） | 单元测试或 curl 验证 |
| 5 | 修改 `src/support-routes.ts`（POST 接入 multer + GET 消息映射加 attachments） | curl 传 multipart 测试 |
| 6 | 修改 `src/admin-routes.ts`（POST 接入 multer + GET 消息映射加 attachments + DELETE 级联清理附件） | 管理后台发图片测试 |
| 7 | 修改 `src/admin-preview.ts`（消息映射加 attachments） | 管理员预览页验证图片显示 |
| 8 | 修改 `src/index.ts` 挂载 `/uploads` + 全局错误处理中间件 | 浏览器访问图片 URL；上传超大文件验证 413 错误 |
| 9 | 修改 `docker-compose.support.yml` | `docker compose up -d` 重建后图片仍在 |
| 10 | 改造前端 `ticket.html` | 顾客端上传图片并查看 |
| 11 | 改造前端 `admin-ticket-preview.html` | 管理员端上传图片并查看 |
| 12 | 添加 `admin-base.css` 图片样式 | UI 检查 |
| 13 | 上线前安全审查 | 尝试上传 SVG、超 5MB 文件、并发 5 请求压测内存、验证路径穿越防护 |

---

## 十、附加优化建议

### 10.1 上传目录大小监控

在 `admin-routes.ts` 或 `admin-logs.html` 中增加一个 API 端点：

```typescript
// GET /api/admin/storage/stats
router.get('/storage/stats', async (req, res) => {
  const agent = await getAgent(req);
  if (!agent) { res.status(401).json({ ok: false, error: '未登录' }); return; }

  const provider = getStorageProvider();
  const totalSize = await provider.getTotalSize();
  res.json({
    ok: true,
    uploadDirSizeBytes: totalSize,
    uploadDirSizeMB: (totalSize / 1024 / 1024).toFixed(2),
  });
});
```

### 10.2 消息删除时级联清理附件文件

当前 `DELETE /api/admin/tickets/:publicTicketNo/messages/:msgId`（`admin-routes.ts` ~834 行）只删除数据库行，不清理磁盘上的附件文件。应在删除前读取 `attachments` 并调用 `provider.delete()`：

```typescript
// DELETE /api/admin/tickets/:publicTicketNo/messages/:msgId — 改造后
router.delete('/tickets/:publicTicketNo/messages/:msgId', async (req, res) => {
  try {
    const msgId = Number(req.params.msgId);
    if (!msgId) { res.status(400).json({ ok: false, error: '无效的消息ID' }); return; }

    // 删除前先查出附件，清理磁盘文件
    const msgR = await query(
      `SELECT attachments FROM support_ticket_messages WHERE id = $1 AND public_ticket_no = $2 AND sender_type = 'agent'`,
      [msgId, req.params.publicTicketNo],
    );
    if ((msgR.rowCount || 0) === 0) {
      res.status(404).json({ ok: false, error: '消息不存在或无权撤回' });
      return;
    }

    // 清理磁盘上的附件文件
    const attachments: string[] = msgR.rows[0]?.attachments || [];
    if (attachments.length > 0) {
      const provider = getStorageProvider();
      for (const url of attachments) {
        await provider.delete(url).catch(() => {});
      }
    }

    // 再删除数据库行
    await query(
      `DELETE FROM support_ticket_messages WHERE id = $1 AND public_ticket_no = $2 AND sender_type = 'agent'`,
      [msgId, req.params.publicTicketNo],
    );

    res.json({ ok: true });
  } catch (e: any) { res.status(500).json({ ok: false, error: e.message }); }
});
```

> 需要额外 import：`import { getStorageProvider } from './storage-service';`

### 10.3 API 文档更新

顾客端 API 响应中 `messages` 数组增加 `attachments` 字段。管理员预览 `admin-preview.ts` 的 `buildCustomerPreviewResponse` 中 `messages.map` 增加 `attachments: m.attachments || []`。

---

## 十一、不做的部分

- ❌ 视频上传（文件太大，售后场景不需要）
- ❌ 云端 OSS 集成（当前量级不需要，已预留接口）
- ❌ 图片内容审核（量级太小、面向海外顾客，暂不需要 NSFW 检测）
- ❌ EXIF GPS 信息剥离（Sharp 的 `rotate()` 已处理方向，GPS 数据在转 WebP 后自动丢弃）
