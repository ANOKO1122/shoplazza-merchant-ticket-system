工单对话加入发送图片的功能，需要限制图片类型大小等，做好安全性，图片可以存本地数据库因为量不大，但要做好压缩

🛠️ 核心设计方案
1. 存储策略：本地持久化（预留 OSS 抽象）
在后端定义一个 StorageProvider 接口。当前实现为 LocalStorageProvider，未来如果业务量暴增，只需写一个 OssStorageProvider 替换即可，不需要改动核心业务逻辑。

2. 压缩策略：后端 Sharp 自动转码与压缩
不要依赖前端压缩（前端容易被绕过且不同设备效果不一）。

统一限制上传原图最大 10MB。

后端使用 sharp 库，统一将图片转换为 WebP 格式（WebP 的压缩率极高，清晰度损失小）。

限制图片最大宽度为 1200px（售后看个衣服破损或订单截图，不需要 4K 分辨率），质量设为 80。压缩后单张图片通常只有 50KB - 150KB。

3. 表结构变更
你需要在 support_ticket_messages（工单消息表）中扩展对图片的支持。
建议将 message_text 保持不变，增加一个 attachments 字段（JSONB 格式），用于存储图片 URL 数组。

SQL
ALTER TABLE support_ticket_messages 
ADD COLUMN attachments JSONB DEFAULT '[]'::jsonb;
存储格式示例： ['/uploads/2026/06/abc123xyz.webp']

💻 后端具体实现步骤
Step 1: 安装依赖
Bash
npm install multer sharp
npm install --save-dev @types/multer
Step 2: 编写存储服务与中间件 (src/storage-service.ts)
这里利用 multer 处理文件上传流，并用 sharp 进行压缩。

TypeScript
import multer from 'multer';
import sharp from 'sharp';
import path from 'path';
import fs from 'fs/promises';
import crypto from 'crypto';

// 1. 配置 Multer 内存存储（不直接落盘，先放内存供 Sharp 压缩）
const upload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 10 * 1024 * 1024 }, // 限制原图最大 10MB
  fileFilter: (req, file, cb) => {
    const allowedTypes = ['image/jpeg', 'image/png', 'image/webp', 'image/gif'];
    if (allowedTypes.includes(file.mimetype)) {
      cb(null, true);
    } else {
      cb(new Error('Invalid file type. Only JPEG, PNG, WEBP and GIF are allowed.'));
    }
  }
});

export const uploadMiddleware = upload.array('images', 3); // 每次最多允许上传 3 张

// 2. 图片处理与保存函数
export async function processAndSaveImages(files: Express.Multer.File[]): Promise<string[]> {
  const uploadDir = path.join(__dirname, '../public/uploads', new Date().getFullYear().toString());
  
  // 确保目录存在
  await fs.mkdir(uploadDir, { recursive: true });

  const savedPaths: string[] = [];

  for (const file of files) {
    // 生成随机文件名，避免重名和中文乱码
    const filename = `${crypto.randomBytes(16).toString('hex')}.webp`;
    const outputPath = path.join(uploadDir, filename);

    // 使用 sharp 压缩并转换为 webp
    let transform = sharp(file.buffer).rotate(); // 自动修正手机拍照的方向

    const metadata = await transform.metadata();
    if (metadata.width && metadata.width > 1200) {
      transform = transform.resize(1200); // 限制最大宽度 1200px
    }

    await transform
      .webp({ quality: 80 }) // 转换为 webp，质量 80
      .toFile(outputPath);

    // 转换为相对于静态目录的 URL 路径
    const relativeUrl = `/uploads/${new Date().getFullYear()}/${filename}`;
    savedPaths.push(relativeUrl);
  }

  return savedPaths;
}
Step 3: 挂载路由与静态目录 (src/index.ts & routes)
在 index.ts 中，确保暴露出静态文件目录（这样前端才能访问到图片）：

TypeScript
// 挂载静态文件目录，使得 /uploads/... 可以被直接访问
app.use('/uploads', express.static(path.join(__dirname, '../public/uploads')));
在 support-routes.ts (顾客端) 和 admin-routes.ts (客服端) 的发消息接口中引入中间件：

TypeScript
import { uploadMiddleware, processAndSaveImages } from './storage-service';
import { ticketService } from './ticket-service';

// 顾客发送消息（带图片）
router.post('/api/support/tickets/:ticketId/messages', uploadMiddleware, async (req, res) => {
  try {
    const { messageText } = req.body;
    let attachments: string[] = [];

    if (req.files && (req.files as Express.Multer.File[]).length > 0) {
      attachments = await processAndSaveImages(req.files as Express.Multer.File[]);
    }

    // 调用你的 ticketService，传入 attachments 存入 DB
    const newMessage = await ticketService.addMessage({
      ticketId: req.params.ticketId,
      text: messageText,
      attachments,
      sender: 'customer'
    });

    res.json({ success: true, data: newMessage });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
🌐 前端改造建议 (ticket.html & admin-base.css)
输入框改造：在发送按钮旁边加一个 📷 图标按钮，点击触发一个隐藏的 <input type="file" multiple accept="image/*">。

预览区域：用户选中图片后，在发送前渲染出小缩略图，并提供 [X] 按钮允许用户取消某张图片。

FormData 发送：因为含有文件，前端请求的 Content-Type 不能再用 application/json，必须改用 FormData 发送：

JavaScript
const formData = new FormData();
formData.append('messageText', textInput.value);
// 循环添加文件
selectedFiles.forEach(file => formData.append('images', file));

await fetch(`/api/support/tickets/${ticketId}/messages`, {
  method: 'POST',
  body: formData // 浏览器会自动设置正确的 Multipart Boundary
});
渲染消息：在渲染对话历史时，读取 message.attachments 数组。如果有 URL，则在消息文本下方渲染 <img src="${url}" class="chat-img" onclick="window.open('${url}')" />。给出简单的 CSS 限制图片显示大小（例如 max-width: 200px; border-radius: 8px;）。

🐳 Docker Compose 部署注意点（关键）
既然你使用了 Docker Compose，绝对不能把图片直接写进容器的独立文件系统中，否则下次 docker compose down && docker compose up -d 重构容器时，用户传的图片就全丢了。

你需要在 docker-compose.yml 中为 support-bridge 服务挂载一个外部 Volume。

修改你的 docker-compose.yml：

YAML
services:
  support-bridge:
    build: .
    ports:
      - "4001:4001"
    volumes:
      # 将宿主机的 ./uploads 目录挂载到容器内静态资源目录
      # 请根据你打包后的实际路径调整容器内路径（例如 /app/public/uploads）
      - ./uploads:/app/public/uploads 
    environment:
      - DB_HOST=postgres
      # ... 其他配置
    depends_on:
      - postgres

 为什么这样做最好？
零成本：不需要为了每周几张图去走云厂商的开户、实名、绑定信用卡流程。

极度轻量：经过 Sharp 的 1200px + WebP 压缩，一张图也就 100KB。一周按 10 张图算，一年也就 50MB 左右的磁盘占用，对服务器毫无压力。

易于迁移：所有的图片都老老实实呆在宿主机的 ./uploads 文件夹里。哪天如果系统真的做大了，你只需要把这个文件夹打包上传到 OSS，然后改写一下 storage-service.ts，把保存逻辑改成云端 SDK 上传，前端和数据库完全不需要重构。