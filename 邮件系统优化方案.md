# 邮件系统优化方案

> 2026-05-19 | 6 项改进（含补发逻辑重构）

---

## 一、补发改为更新原任务（不再创建子任务）

### 旧方案
每次补发 INSERT 一条新记录（`manual_resend_of = 原任务ID`），任务号加后缀 `-1`, `-2`：
```
EJ202605181825245e7404-3  ← 补发 #3（子任务）
EJ202605181825245e7404-2  ← 补发 #2（子任务）
EJ202605181825245e7404-1  ← 补发 #1（子任务）
EJ202605181825245e7404    ← 原任务（状态僵死不变）
```

**问题**：原任务状态不变（如一直显示 `failed`），列表膨胀，父子关系复杂。

### 新方案（✅ 已实施）
**不创建子任务，直接 UPDATE 原任务：**
- `status` → `pending`
- `scheduled_at` → `now()`（立即发送）
- `retry_count` → `retry_count + 1`
- `last_error` / `failed_at` / `sent_at` → `NULL`

任务号始终不变，一条任务贯穿整个生命周期。管理员通过 `retry_count` 和操作日志了解补发历史。

### 实现
- `resendEmailJob(id)`: 
  - `pending` → 直接触发立即发送
  - `failed` / `cancelled` / `skipped` → 重置为 `pending` 后立即发送
  - `sent` / `sending` → 拒绝（已成功或发送中）
- `cancelEmailJob(id)`: 允许取消 `pending` 和 `failed` 状态
- 前端不再查询/渲染 `manual_resend_of` 子任务

---

## 二、按状态显示操作按钮

### 规则

| 状态 | 按钮 |
|------|------|
| `pending`（待发送） | **发送**（立即发送）+ **取消** |
| `failed`（发送失败） | **重发** + **取消** |
| `cancelled`（已取消） | **重发** |
| `sent`（已发送） | 无按钮 |
| `sending`（发送中） | 无按钮 |
| `skipped`（已跳过） | 无按钮 |

### 说明
- 已取消的任务**可以**重发（与初版设计不同，更加灵活）
- 已成功发送的任务不可再次重发
- "发送"按钮适用于待发送任务想立即发出的场景（不等调度）

---

## 三、统一客户端链接

### 现状
每次补发生成新的 bootstrap token → 顾客收到不同的链接。

### 改为
- 原任务创建时生成 `bootstrap_token`，存于 `support_access_tokens`
- 补发（重置）**复用**同一个 token，不新建
- 管理员可**手动重新生成**链接（单独按钮"重新生成链接"，会使旧 token 失效）
- `email_jobs.token_id` 统一指向同一个 `support_access_tokens.id`

### 确认点
> "重新生成链接"后，旧链接是否立即失效？建议：旧 token 标记 `revoked_at`，新 token 立即生效。
答：管理员重新生成后，旧链接立即失效，使用新链接

---

## 四、待发送邮件预览 + 可编辑

### 现状
`status=pending` 时，`subject_snapshot` 和 `body_snapshot` 为空（发送时才渲染）。

### 改为
- 邮件任务创建时**立即渲染**快照（不等到发送）
- 详情面板：不管什么状态都显示主题/正文快照
- 待发送状态下，快照区域可编辑：
  - 主题：`<input>` 可修改
  - 正文：`<textarea>` 可修改
  - 保存后更新 `subject_snapshot` / `body_snapshot`
- 已发送/已取消：只读展示

### 确认点
> 编辑保存后，实际发送用的是编辑后的内容？→ 是，`sendEmailJob` 里 `subject_snapshot` / `body_snapshot` 非空时优先使用。
答：发送使用在详情快照里编辑后的实际内容

---

## 五、邮件模板编辑页

### 位置
侧边栏：`工单处理 | 店铺管理 | 订单邮件管理 | 📧 邮件模板 | 系统日志`

### 功能
- 独立页面（`public/email-templates.html`）
- 三种模板可编辑：
  1. **下单/付款后通知** (`paid_support_invite`)
  2. **客服回复提醒** (`agent_reply_notice`)
  3. **工单关闭提醒** (`ticket_closed_notice`)
- 每套模板包含：
  - 邮件主题模板（支持变量：`{{store_name}}`、`{{order_number}}`、`{{public_ticket_no}}`、卡号后四位，支付方式、店铺域名、下单时间、顾客名称等等）
  - 邮件正文模板（HTML 编辑器）
- **预览功能**：填入测试数据（店名、订单号等），实时渲染预览
- 模板存储：新建 `support_email_templates` 表，默认值硬编码
- API：
  - `GET /api/admin/email-templates` — 列出所有模板
  - `GET /api/admin/email-templates/:type` — 获取单个模板
  - `PUT /api/admin/email-templates/:type` — 更新模板
  - `POST /api/admin/email-templates/:type/preview` — 预览渲染

### 表结构
```sql
CREATE TABLE support_email_templates (
  email_type TEXT PRIMARY KEY,
  subject_template TEXT NOT NULL,
  body_template TEXT NOT NULL,
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

---

## 确认清单

| # | 问题 | 你的决定 |
|---|------|----------|
| 1 | 补发创建子任务 → 改为更新原任务 | ✅ 确认 |
| 2 | 已取消的原任务能否补发？ | 能（重发按钮） |
| 3 | 重新生成链接 → 旧链接立即失效？ | 是 |
| 4 | 待发送编辑的内容直接用于发送？ | 是 |
| 5 | 模板编辑器用纯文本还是 HTML 编辑器？ | HTML（灵活） |
| 6 | 模板编辑页放独立文件 `email-templates.html`？ | 是 |


## 进度

| # | 需求 | 状态 |
|---|------|------|
| 1 | 补发更新原任务（不再创建子任务） | ✅ |
| 2 | 按状态显示按钮（发送/重发/取消） | ✅ |
| 3 | 统一链接，补发复用 token | ✅ |
| 4 | 待发送即时渲染快照 | ✅ |
| 5 | 模板编辑页 | ⏳ 下一步 |

### 已改动文件

| 文件 | 改动 |
|------|------|
| `src/email-service.ts` | `resendEmailJob` 重写为 UPDATE 原任务；`cancelEmailJob` 支持 `pending`/`failed`；移除子任务创建逻辑 |
| `src/admin-routes.ts` | 取消路由错误信息更新；resend 日志适配新逻辑 |
| `public/admin-emails.html` | 按钮按状态切换（发送/重发/取消）；移除子任务/补发历史渲染 |